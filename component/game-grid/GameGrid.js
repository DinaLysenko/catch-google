export function GameGrid(){
    let grid=document.createElement('table')
    let row=document.createElement('tr')
    for (let x = 0; x < 4; x++) {
        
    }
    grid.append(row)
    return grid
}